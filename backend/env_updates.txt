# ============== ADICIONE ESTAS LINHAS NO SEU .env ==============
# Copie e cole no final do seu arquivo .env existente
# NÃO APAGUE as linhas que já existem!

# ============== NOTIFICAÇÕES WHATSAPP (TWILIO) ==============
# Durante desenvolvimento, deixe ENABLE_WHATSAPP=false
# Em produção com número Twilio real, mude para true
ENABLE_WHATSAPP=false

# Configure apenas se for usar WhatsApp (ENABLE_WHATSAPP=true)
# 1. Criar conta: https://www.twilio.com/try-twilio
# 2. Console → Account Info → copie SID e Auth Token
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=sua_auth_token_aqui
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886

# ============== PUSH NOTIFICATIONS ==============
# OBRIGATÓRIO! Gere com o comando:
# python -c "from pywebpush import Vapid; v = Vapid(); v.generate_keys(); print('VAPID_PRIVATE_KEY=' + v.private_key.decode()); print('VAPID_PUBLIC_KEY=' + v.public_key.decode())"

VAPID_PRIVATE_KEY=sua-chave-privada-vapid-aqui
VAPID_PUBLIC_KEY=sua-chave-publica-vapid-aqui

# URL do serviço de push (mantenha assim em desenvolvimento)
PUSH_SERVICE_URL=http://localhost:8001
